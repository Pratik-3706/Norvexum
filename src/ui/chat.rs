// Chat rendering helpers (future: full markdown rendering)
pub fn format_markdown(_text: &str) -> String {
    // TODO: Implement terminal markdown rendering
    // For now, pass through as-is
    _text.to_string()
}
